$('#total').html("<%= @puntaje_total %>");
$('#etapa_change').html("<%= @etapa %>");
$('#campos_calif').html("<%= escape_javascript( render 'campos_calificaciones', calificaciones: @calificaciones, materia: @materia, puntaje_total: @puntaje_total, etapa: @etapa ) %>");