<div class="modal-header">  
    <a class="close" data-dismiss="modal">×</a>  
    <h3>Subir Material</h3>  
  </div>  
  <div class="modal-body">  
    <p id="error_material" class="error"></p>
    <%= form_for(:material, remote: true) do |form| %>
      <%= form.text_field :materia_id, value: materia.id, class: "label", :style => 'display:none;' %>
      <%= form.label :file, 'Subir material', class: 'label' %>
      <%= form.file_field :file %><br/>
  </div>  
  <div class="modal-footer"> 
      <%= form.submit "Guardar", :class => 'btn btn-success' %> 
    <a href="#" class="btn" data-dismiss="modal">Cerrar</a>  
  </div>  
      <% end %>