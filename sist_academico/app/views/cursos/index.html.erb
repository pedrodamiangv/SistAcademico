<div class="container">
  <div class="row">
    <h1 class="h1">Lista de Cursos</h1>
    <div class="row" align="right">
      Buscar:<input type="text" id="kwd_search" value="" class="span3 seguido search-query"/> 
    </div>
    <div class="row">
      <table id="my-table" class="table table-bordered">
        <thead class="table-title">
          <tr>
            <th>Curso</th>
            <th>Turno</th>
            <th>Nivel</th>
            <th>Enfasis</th>
            <% if current_user.is_administrativo %>
              <th></th>
            <% end %>
          </tr>
        </thead>
        <tbody>
          <% @cursos.each do |curso| %>
            <tr>
              <td><%= link_to curso.curso, curso %></td>
              <td><%= curso.turno %></td>
              <td><%= curso.nivel %></td>
              <td><%= curso.enfasis %></td>
              <% if current_user.is_administrativo %>
                <td><%= link_to 'Editar', edit_curso_path(curso) %>
                <%= link_to 'Eliminar', curso, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= will_paginate @cursos %>
      <% if current_user.is_administrativo %>
        <%= link_to 'Registrar Curso', new_curso_path %>
      <% end %>
    </div>
  </div>
</div>

