  <div class="modal-header">  
    <a class="close" data-dismiss="modal">×</a>  
    <h3>Registrar Direccion</h3>  
  </div>  
  <div class="modal-body">  
    <p id="error" class="error"></p>
    <%= form_for direccion, :remote => true do |form| %>
      <%= form.text_field :tipo, value: 'alumnos', style: 'display: none;' %>
      <%= form.label :direccion %>
      <%= form.text_field :direccion %>
      <%= form.label :barrio %>
      <%= form.text_field :barrio %>
      <%= form.label :city, "Ciudad" %>
      <%= form.collection_select :city_id ,@cities,:id,:city %>
  </div>  
  <div class="modal-footer"> 
      <%= form.submit "Guardar", :class => 'btn btn-success btn-large-primary' %> 
    <a href="#" class="btn btn-large btn-default" data-dismiss="modal">Cerrar</a>  
  </div>  
      <% end %>