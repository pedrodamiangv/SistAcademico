<p id="notice"><%= notice %></p>
<div class="datos">

<p>
  <b>Ciudad:</b>
  <%= @city.city %>
</p>

<p>
  <b>Pais:</b>
  <%= @city.country_pais %>
</p>

<div id="vinculos">
	<% if current_user.is_administrativo %>
  		<i class="icon-edit"></i><%= link_to 'Editar', edit_city_path(@city) %> |
	<% end %>
		<i class="icon-list"></i><%= link_to 'Listar', cities_path %>
</div>
</div>
